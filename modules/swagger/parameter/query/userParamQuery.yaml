###################################################
################## [QueryString] ##################
###################################################
components:
  parameters:
    # 테스트 계정 로그인
    userLoginTest:
      in: query
      name: testNum
      required: true
      description: 테스트 계정 번호
      example: 1
      schema:
        type: integer
        format: int64